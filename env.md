# 환경 변수 설정

앱은 **프로젝트 루트의 `.env` 파일**만 읽습니다. `env.md`는 안내용이며, 여기에 적은 값은 Vite에 자동으로 반영되지 **않습니다**.

## 필수

프로젝트 루트에 `.env` 파일을 만들고 한 줄로 넣으세요 (실제 키로 바꾸세요).

```
VITE_OPENAI_API_KEY=sk-여기에_OpenAI_API_키
```

Vite는 `VITE_`로 시작하는 변수만 브라우저 코드에 포함합니다. `OPENAI_API_KEY`만 쓰면 동작하지 않습니다.

## 선택

```
VITE_OPENAI_MODEL=gpt-4o-mini
```

지정하지 않으면 기본값은 `gpt-4o-mini`입니다.

## 사용 방법

1. 루트에 `.env`를 만들고 `VITE_OPENAI_API_KEY`를 설정합니다.
2. `npm install` 후 `npm run dev`를 실행합니다.
3. `.env`를 수정한 뒤에는 개발 서버를 **다시 시작**합니다.

## 보안 참고

- `env.md`는 Git에 올릴 수 있으니 **실제 API 키는 넣지 마세요.**
- `VITE_*` 변수는 빌드 결과에 포함되어 브라우저에서 노출됩니다. 공개 배포 시에는 백엔드 프록시를 쓰는 편이 안전합니다.
- 키가 문서나 저장소에 노출되었다면 OpenAI 대시보드에서 키를 **재발급·폐기**하는 것을 권장합니다.
